# Team Divider - 更新ログ

## v1.17（2026-04-29）

LoL 専用ツールへ差し戻し。レーン機能の追加。

### LoL 化

- **タイトル**: `TEAM DIVIDER` → `LOL TEAM DIVIDER`
- **サブタイトル**: `BALANCED MATCHMAKING SYSTEM` → `LEAGUE OF LEGENDS CUSTOM MATCH`

### レーン機能

- **5レーン対応**: `TOP / JG / MID / ADC / SUP`（色分けバッジ）
- **担当レーン（プレイヤー属性）**: `player.lanes` に複数選択可能
  - 参加者登録時にトグルで選択（既定: 全5レーン）
  - 既存プレイヤーは編集モーダルから変更可能
  - 担当レーン空 = 全レーンOK（柔軟に割当）
- **レーン編集モーダル**: 参加者リストの「ALL/TOP/MID等」ボタンクリックで開く
  - 各レーンのトグルボタン、全選択 / 全解除のクイック操作
- **自動レーン割当アルゴリズム** (`assignLanes`)
  - 1チーム5人 × 5レーンの全順列（120通り）を総当たり
  - 各プレイヤーの希望度合計が最大の組合せを採用
  - 同点はランダムで分散（毎ラウンド変化を出す）
  - 両チーム独立で最適化
- **チーム表示の改善**
  - レーン順 (TOP→JG→MID→ADC→SUP) でソート表示
  - プレイヤー番号の代わりに**レーンバッジ**を表示
  - 担当外レーンに割り当てられた場合は ⚠ 警告マーク
- **リロール / D&D / タップスワップ時もレーン再割当**
  - チーム構成変更後、自動的に最適レーンを再計算

### スキーマ

- v4 → v5: `player.lanes` / `round.lanes` を追加
- 既存データの自動マイグレーションで `lanes: ['TOP','JG','MID','ADC','SUP']` を補完

---

## v1.16（2026-04-29）

ロジック周りのリファクタリング。動作仕様は変更なし。

### 共通処理の集約

- **`commitRoundChange(round, opts)`** を新設
  - ラウンド更新後の定型処理（キャッシュ無効化 → 保存 → ボタン状態 → 描画 → 重複検出）を1関数に集約
  - generateRound / setWinner / rerollRound / onDrop / onSwapTap / resetRounds で利用
  - 約60行の重複コードを削減
- **`swapInRound(round, srcLoc, srcId, dstLoc, dstId)`** を新設
  - D&D とタップスワップで重複していたID単位のスワップ処理を統合
  - 「勝敗判定モード時に勝者をクリア」「selectedIds 再計算」もここに集約
- **`optimizationScore(team1, team2)`** を新設
  - チーム分け最適化スコアの計算を統一（過去の同チームペア + 勝率差 \* 2）
  - `optimizeSameRankSwaps` と `balanceByRankSum` Pass 2 で同関数を参照

### パフォーマンス: 統計キャッシュ

- **getPlayCount / getSpecCount / getWinCount / getLossCount / getLastPlayedIndex** をキャッシュ越しに変更
  - 旧実装は呼出し毎に rounds 全走査していた（描画1回で数百回スキャン）
  - キャッシュ層 (`_statsCache`, Map<id, stats>) で1回の走査で全プレイヤー分集計
  - rounds / players が変化したら `invalidateStatsCache()` で次回アクセス時に再構築
  - 描画の重さが体感できるレベル（〜数十ラウンド × 20人規模）で改善

### スキーマバージョン

- `localStorage` 保存データに **`schemaVersion`** を明示的に保存
- `migrateData(data)` で旧バージョンを順次最新形式へマイグレーション
  - v1: 〜v1.10（players/pid のみ）
  - v2: v1.11〜（lockGroup 追加）
  - v3: v1.13〜（rounds/winRateMode/winner 追加）
  - v4: v1.16〜（schemaVersion 明示）
- 将来の破壊的変更時に互換性を維持しやすい構造に

### 整理

- バグ修正: balanceByRankSum Pass 2 が古い `calcWinDiff` を参照したまま残っていた問題を修正
  （v1.15 の更新が片側のみだった）
- 未使用関数の削除: `calcWinDiff` / `calcTeamWins`（v1.15 で `calcWinRateScore` に置換済み）
- 未使用 CSS の削除: `.btn-flag` / `.btn-flag.on-pri` / `.btn-flag.on-brk` / `.btn-lock` 系
  （v1.14 で `.icon-btn` に統合済み）

### 残課題（次回以降）

- **内部 `rank: 0,1,2` → `1,2,3` 統一** は今回見送り。影響範囲が広く（30箇所超のインデックス算術）、
  独立した PR として扱うのが安全と判断。HANDOVER に長期 TODO として残置。

---

## v1.15（2026-04-29）

勝率均等化ロジックの改善。

### アルゴリズム

- **勝利数差ベース → 平均勝率差ベース に変更**
  - 旧: `Math.abs(team1の勝利数合計 - team2の勝利数合計)`（サンプル数の偏りに弱い）
  - 新: `Math.abs(team1の平均勝率 - team2の平均勝率)`（チームの強さを公平に比較）
  - **狙い**: 両チームの平均勝率を等しくすると結果が確率的に分散し、長期的に全プレイヤーの勝率が 50% へ収束する
- **Laplace スムージング（α=1）を導入**
  - `winRate = (wins + 1) / (wins + losses + 2)`
  - 0戦の人は 0.5 として扱い、サンプル少の人もノイズに引きずられない
- **重み係数を調整**: `* 5` → `* 2`（スコア値の桁が変わったため）
- 適用箇所は従来通り `optimizeSameRankSwaps` と `balanceByRankSum` Pass 2

### 表示

- **チームヘッダに「勝率 XX%」バッジ**を追加（勝敗判定モードON時のみ）
  - 両チームの平均勝率が一目で確認可能
- **W-L pill のツールチップに勝率%を表示**
  - ホバーで「3勝2敗 / 勝率60%」を確認できる

---

## v1.14（2026-04-29）

UI 全面見直し。コンパクト化・操作性向上・モバイル対応。

### モード設定

- **モードカードを横並びコンパクトレイアウトに刷新**
  - チーム分け / 選出方式 / 選出傾向 / 勝敗判定の4行構成
  - ラジオボタンを **トグルピル** に変更（クリック範囲拡大、視認性向上）
  - 説明文を tooltip に逃がして高さ削減
  - 「選出傾向: なし」→ 「**均等**」にリネーム（より分かりやすく）
  - 勝敗判定モードを **スイッチ** に変更（ON/OFFが直感的）

### 操作行

- **設定メニュー（︙）を追加**
  - 「ラウンド履歴リセット」「保存データ全削除」「操作ヘルプ」をメニュー化
  - メイン操作行をスッキリさせ、誤クリック事故を防止
- **「勝者入力待ち」ステータスピル**を生成ボタン横に表示
  - 勝敗判定モード時にブロック理由を一目で把握できる

### サイドバー（参加者リスト）

- **行の情報密度を改善**
  - 操作ボタンを **アイコンのみ** に変更（★優先・👁待機・🔒・×）
  - active 時は色付きで状態が分かる
  - title 属性で操作内容のツールチップ表示
- **行内ソートを追加**: 優先 → 通常 → 待機（onBreak）の順
- **長い名前を title で全文表示**（マウスホバー時）
- **凡例フッタを撤廃**、代わりに「ヘルプを見る」リンク
- 凡例内容は **ヘルプモーダル** へ集約

### ヘルプモーダル

- 操作・モード・アイコン・凡例を一覧化
- 設定メニューやサイドバーから呼び出し可能
- 必要な時だけ参照できるので、画面の常時占有を解消

### ラウンド表示

- 「合計 9」→「**レシオ計 9**」にラベル明確化
- **D&D / タップ操作のヒント** を待機者セクションに追加
- **タップで2人スワップ機能** を追加（モバイル D&D 代替）
  - プレイヤーをタップ → 別プレイヤーをタップ → 入替
  - タップ選択中はゴールド枠でハイライト
  - 同じものを再タップで選択解除
- **重複ラウンド警告をクリッカブル化**: クリックで該当履歴へスクロール
- **同順 vs B/R逆転** を区別して表示（「同じ組み合わせ」/「同じ顔ぶれ（B/R逆転）」）

### 勝敗判定モード

- **W-L ピルを勝率に応じて色分け**
  - 30%未満: 赤系 / 30-50%: 橙 / 50-70%: 緑 / 70%以上: 青
- 勝者UIの「変更」ボタンを「**やり直し**」（アイコン付き）に変更
- 履歴に **大きな勝者バッジ** + 左ボーダーで勝者色分け

### モバイル / アクセシビリティ

- HTML5 D&D が効かないタッチデバイス向けに **タップスワップ** で代替
- アイコンボタンに title 属性で機能説明
- ヘルプモーダルでアクセス可能

---

## v1.13（2026-04-29）

勝敗判定モードの追加。

### 機能追加

- **勝敗判定モード（チェックボックスでON/OFF切替）**
  - ラウンド生成後、BLUE / RED どちらが勝利したか入力するUIを表示
  - 未入力のまま「次のラウンド生成」を押すとブロック（ボタン無効化＋警告）
  - 各プレイヤーの勝利数・敗北数をラウンド履歴から動的算出
  - チーム分けスワップ最適化に勝利数差を組み込み、両チームの勝利数を均等化
  - プレイヤーリスト・チーム表示・履歴に勝-負スタッツを表示（モードON時のみ）
  - 「変更」ボタンで勝者を再設定可能
- **ラウンド履歴を localStorage に永続化**
  - 勝敗カウントの算出ソースとなるため、ブラウザ再読込でも履歴を保持
  - 後方互換: 古いデータには `winner: null` を補完

### アルゴリズム

- `optimizeSameRankSwaps`（均等レシオ分け用）の評価関数に勝利数差を加算
- `balanceByRankSum` Pass 2 の評価関数に勝利数差を加算
- 勝敗判定モードOFFの場合は従来動作（影響なし）

### リロール・D&D との連携

- リロールやドラッグ＆ドロップでチーム構成が変わった場合、勝者を自動クリア
- 構成変更後は再度勝者入力が必要

---

## v1.12（2026-04-29）

ドラッグ＆ドロップ手動入替機能の追加と、バランス優先時の選出ロジック改善。

### 機能追加

- **チーム⇔待機者のドラッグ＆ドロップ入替**
  - ラウンド生成後、BLUE / RED / 待機者 のプレイヤーをドラッグ操作で入替可能
  - プレイヤーを別のプレイヤーにドロップすると ID をスワップ（チーム人数は自動維持）
  - 視覚フィードバック: ドラッグ中はターゲットを破線アウトラインでハイライト
  - 入替後に重複ラウンド検出を再実行（過去と同じ組み合わせなら警告）

### 機能追加（追補）

- **選出傾向に「なし」オプションを追加（デフォルト）**
  - 低レシオ優先・高レシオ優先のどちらにも偏らない、ニュートラルな選出
  - バイアス補正がゼロになり、構成選択は参加回数バランス調整のみに従う
  - 新規ラウンド生成のデフォルトを「なし」に変更

### バグ修正

- **バランス優先時に参加回数が考慮されない問題を修正**
  - レシオグループ内ソートが `getLastPlayedIndex` のみで、参加回数の少ない人を優先できていなかった
  - 新しい優先順位: ①前ラウンド待機者 → ②参加回数が少ない順 → ③待機回数が多い順 → ④最後の参加が古い順 → ⑤ランダム
  - グループ内の選択肢がある場面（在籍人数 > 構成数）で、待機が偏らないようになる
- **構成選択（レシオ人数の組み合わせ）に参加回数バランス調整を追加**
  - 各レシオの平均参加回数を算出し、平均より参加が多いレシオを多く含む構成にペナルティ
  - 例: レシオ1 在籍5人で全員 27回 / レシオ2 在籍8人で 17回の場合、
    従来は (4,4,2) が約75%選ばれていたが、調整後は (2,6,2) が優先される
  - 参加回数が全員均等な場合は playFactor=1 で影響なし
  - バイアス設定（低/高レシオ優先）は引き続き有効、補正として作用

---

## v1.11（2026-04-28）

ロックグループ機能の追加。

### 機能追加

- **ロックグループ（同チーム強制）**
  - 各プレイヤーに 🔒A / 🔒B / 🔒C のロックグループを設定可能
  - 同じグループに所属するプレイヤーは必ず同じチームに配属される
  - 参加者リストの 🔒 ボタンクリックで なし→A→B→C→なし のサイクル変更
  - チーム表示にロックバッジを表示
  - グループごとに色分け（A=パープル / B=ティール / C=オレンジ）
  - localStorage 復元時の後方互換あり（既存データの lockGroup は 0 として扱う）

### アルゴリズム

- 既存のチーム分けアルゴリズム（均等レシオ分け / レシオ合計均等）の後段に
  `consolidateLockGroups` を追加し、分散したロックメンバーを多数派側に寄せる
- スワップ最適化（`optimizeSameRankSwaps` / Pass 2）に
  ロックグループ分断防止ガード（`wouldSplitLockGroup`）を追加
- 寄せきれない場合（グループ人数 > 5 等）はトーストで警告

---

## v1.10（2026-03-27）

汎用化・選出ロジック強化・UI改善。

### 機能追加

- **選出方式オプション**
  - バランス優先（デフォルト）: レシオの均衡を重視し、連戦を許可
  - ローテーション優先: 全員が均等に参加することを重視
- **選出傾向オプション（バランス優先時）**
  - 低レシオ優先（デフォルト）/ 高レシオ優先の2択
  - 同スコアの構成間でバイアス付きランダム選択
- **各レシオから最低1名選出オプション（デフォルトON）**
  - バランス優先・ローテーション優先の両方で有効
  - 候補なしの場合は自動フォールバック
- **重複ラウンド検出**
  - 過去ラウンドと同じチーム構成（Blue/Red逆転含む）を検出し、バッジ＋トーストで警告
- **レシオのクリック変更**
  - 参加者リストのレシオバッジをクリックで 1→2→3→1 とサイクル変更
- **レシオ別人数の表示**
  - 参加者リストのヘッダーにレシオ別の人数バッジを表示

### バグ修正

- **バランス優先時にレシオ差が生まれる問題を修正**
  - 構成の全列挙＋スコアリングでレシオ合計が偶数になる構成を選出
  - 均等レシオ分け時は各レシオの人数が偶数になる構成を優先
- **スネークドラフトでレシオ差が蓄積する問題を修正**
  - 2パス構成に変更: Pass1でレシオ差を最小化、Pass2でレシオ差を維持しつつ同チームペアを最小化
- **リロール時に選出方式・チーム分けモードが反映されない問題を修正**
  - チェックON時は generateRound と同じ選出ロジックを通すよう変更
- **レシオ合計の表示値が内部値（0,1,2）で計算されていた問題を修正**
  - 表示用の calcRankSum で rank+1 を使用するよう変更
- **HTMLのdivタグ不整合によるレイアウト崩れを修正**

### 表記変更

- LoL 関連の表記を全て削除（汎用ツール化）
- 「ランク」→「レシオ」に統一
- レシオ表示を「初・中・上」→「1・2・3」に変更（バッジを丸型に統一）
- 「観戦」→「待機」に統一
- ファイル名を `lol-team-divider.html` → `team-divider.html` に変更
- ヘッダー・タイトルを `TEAM DIVIDER` に変更

### UI改善

- 参加者登録のレシオボタンに「レシオ」ラベルを追加
- `.player-scroll` / `.history-scroll` をビューポート基準の高さに変更
- 選出方式がローテーション時、選出傾向オプションを自動非表示
- リロールボタンとチェックボックスをグループ化し視覚的に紐付け
- ラウンド履歴に参加者のレシオバッジとチームレシオ合計を表示

### 技術ノート

- 内部ではレシオを `0, 1, 2` で管理（初級/中級/上級時代の名残）
- 表示上は `1, 2, 3` に変換、合計値の計算には `rank + 1` を使用
- ソースコード内のコメントにも注記を追加

---

## v1.00（2026-03-27）

リファクタリング・バグ修正を経た初回安定版。

### バグ修正

- **リロール時に待機者が消失する問題を修正**
  - チェックOFF でリロールを繰り返すと、枠不足で待機になった人が specs から消えて二度と戻れなくなるバグを修正
  - specs の算出を「プールからのスライス」ではなく「チームに入らなかった全員」から算出するよう変更
- **リロール時に★優先参加者が待機者に落ちる問題を修正**
  - シャッフル後の先頭取得で優先者が後ろに回る可能性があったため、優先者を先にチーム枠に確保するよう変更

### リファクタリング

- 変数名・関数名を明確化
  - `pid` → `nextPlayerId`, `e()` → `escapeHtml()`, `prow()` → `renderPlayerRow()` 等
  - `balanceRC()` → `balanceByRankCount()`, `stScore()` → `calcSameTeamScore()` 等
- ヘルパー関数の追加（`findPlayer()`, `getSelectedMode()`, `showRerollControls()`）
- 選出優先度ソートを `sortBySelectionPriority()` に分離
- 同レシオスワップ最適化を `optimizeSameRankSwaps()` に分離
- `sort(() => Math.random() - 0.5)` → Fisher-Yates シャッフルに置換
- セクションコメント・JSDoc コメントの整備

### その他

- ヘッダーにバージョン表記を追加
- リロールチェックボックスのデフォルトを OFF に変更

---

## v1.00 以前の開発履歴

初回リリースに至るまでの開発経緯。

### 初期実装

- 参加者登録（最大 20 人、レシオ 3 段階）
- 5v5 チーム自動生成（スネークドラフト方式）
- 待機者ローテーション・優先参加フラグ
- 過去ラウンドの同チームペア重複を最小化する最適化ループ
- ラウンド履歴表示

### Bootstrap 5 対応・待機者ローテバグ修正

- Bootstrap 5.3.3 を導入
- 待機者が同じ人に偏るバグを修正（登録順バイアス）
  - ソート条件に累計待機回数を追加

### UI 改善・機能追加

- 白背景（#FFF）ベースの配色に変更
- 各プレイヤーの参加数・待機数を表示
- ラウンド履歴に待機者・使用モードを追加
- チーム分けモードの追加
  - 「均等レシオ分け」を標準モードとして追加（各レシオの人数を均等に分配）
  - 既存のスネークドラフト方式を「レシオ合計均等」オプションに変更
- 👁待機機能を追加（今ラウンドを欠席し待機者になる）

### リロール機能

- ラウンド生成後にチーム割り当てを再抽選するリロール機能を追加
- リロール回数の表示（表示部・履歴の両方）

### データ永続化

- localStorage による参加者データの自動保存・復元
- 保存データ削除ボタンの追加

### リロール改善

- リロール時にモード切替を可能に（生成時と別モードでリロール）
- 枠不足の待機者をリロールに含めるチェックボックスを追加
- 登録順バイアスの除去（ソート最終段にランダムタイブレーカー追加）

### 表記統一

- 「休憩」の文言を全て「待機」に統一
